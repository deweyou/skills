# Claude Code Agent Preset

Used by `scripts/run.js` when `--agent claude` or auto-detection selects Claude Code.

```agent-routing
claude --print "$(cat {PROMPT_FILE})"
```

```agent-execute
claude --print "$(cat {PROMPT_FILE})"
```

```grader
claude --print "$(cat {PROMPT_FILE})"
```
